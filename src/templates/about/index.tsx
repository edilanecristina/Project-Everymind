import { FC } from "react";
import { AboutNunesSports } from "./textSection";

export const RootAboutPage: FC = () => {
  return (
    <section id="content">
      <AboutNunesSports />
    </section>
  );
};
